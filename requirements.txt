# requirements.txt
MDAnalysis[analysis]
plyfile

numpy
scipy
pandas
biopython
prody
spyrmsd
rdkit-pypi
tokenizers
transformers
huggingface-hub
wandb
e3nn
scikit-learn
accelerate

# Core dependencies from environment.yaml (pip equivalents)
#openff-toolkit==0.15.2
#openff-interchange==0.3.25
#openff-forcefields==2024.3.0
#openff-units==0.2.2
#openff-utilities==0.1.12
#openff-models==0.1.2
#smirnoff99frosst==1.1.0
# openff-toolkit==0.15.2 is not available on PyPI — it is only distributed via Conda channels (like conda-forge), which pip cannot install from.

#clean inference-only container, not running parameterization, SMIRNOFF typing, or simulation prep
# Pinned to latest PyPI-available versions
openff-toolkit==0.14.5
openff-units==0.2.2
openff-utilities==0.1.12

# Required for forcefield and molecule handling
openbabel==3.1.1
loguru==0.7.2
prefetch-generator==1.0.3
biopython==1.79
